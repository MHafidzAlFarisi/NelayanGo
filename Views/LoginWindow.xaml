<Window x:Class="NelayanGo.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Nelayan Go - Login"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="768" Width="1366"
        Background="#FF76A8D8">

    <Window.Resources>

        <!-- Style TextBox & PasswordBox biar rounded -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="#222" />
            <Setter Property="Background" Value="#80D0E4F5" />
            <Setter Property="BorderBrush" Value="#083A66" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Foreground" Value="#222" />
            <Setter Property="Background" Value="#80D0E4F5" />
            <Setter Property="BorderBrush" Value="#083A66" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tombol utama (Login) -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="20">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#295189" Offset="0" />
                                    <GradientStop Color="#0B325F" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Height" Value="40"/>
        </Style>

        <!-- Tombol Google -->
        <Style x:Key="GoogleButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF345CA8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="20"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="10,2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Height" Value="40"/>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- BACKGROUND FOTO -->
        <Grid.Background>
            <ImageBrush ImageSource="Assets/bg_login.png"
                        Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- LINGKARAN-LINGKARAN BIRU DI BELAKANG CARD -->
        <Ellipse Width="600" Height="600"
                 Fill="#40FFFFFF"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Bottom"
                 Margin="-150,0,0,-120"/>

        <Ellipse Width="650" Height="650"
                 Fill="#40367DB0"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Margin="0,40,0,0"/>

        <Ellipse Width="550" Height="550"
                 Fill="#402E79A7"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Bottom"
                 Margin="0,0,-120,-80"/>

        <!-- CARD LOGIN -->
        <Border Width="380" Padding="30"
                CornerRadius="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="#26000000"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#E3EFFB" Offset="0"/>
                    <GradientStop Color="#8CB6D8" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#55000000"
                                  ShadowDepth="6"
                                  BlurRadius="18"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- LOGO & TITLE -->
                <StackPanel Orientation="Horizontal"
                            Grid.Row="0"
                            Margin="0,0,0,10">
                    <Image Source="Assets/logo_nelayan_go.png"
                           Height="40"
                           Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="NELAYAN GO"
                                   FontWeight="SemiBold"
                                   FontSize="12"
                                   Foreground="#083A66"/>
                    </StackPanel>
                </StackPanel>

                <!-- FORM LOGIN -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="Halo!"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="#131313"
                               Margin="0,0,0,14"/>

                    <TextBlock Text="Email"
                               FontSize="13"
                               Margin="0,4,0,2"
                               Foreground="#333"/>
                    <TextBox Style="{StaticResource RoundedTextBoxStyle}"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             Height="34"/>

                    <TextBlock Text="Password"
                               FontSize="13"
                               Margin="0,10,0,2"
                               Foreground="#333"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Style="{StaticResource RoundedPasswordBoxStyle}"
                                 Height="34"
                                 PasswordChanged="PasswordBox_PasswordChanged"/>

                    <Button Content="Login"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="0,20,0,0"
                            Command="{Binding LoginCommand}"/>

                    <TextBlock Text="or"
                               FontSize="12"
                               Foreground="#444"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,4"/>

                    <Button Style="{StaticResource GoogleButtonStyle}">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Image Source="Assets/google_icon.png"
                                   Height="18"
                                   Margin="0,0,8,0"/>
                            <TextBlock Text="Sign In With Google"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- LINK DAFTAR -->
                <TextBlock Grid.Row="2"
                           HorizontalAlignment="Center"
                           Margin="0,14,0,0"
                           FontSize="11"
                           Foreground="#173D6C">
                    <Run Text="Belum punya akun? "/>
                    <Hyperlink Click="RegisterHyperlink_Click"
                               Foreground="#173D6C">
                        Daftar
                    </Hyperlink>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
